<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit book</title>
</head>
<body>
    <h1>Edit book</h1>
    <% if(locals.errors) {%> 
        <ul>
            <% locals.errors.forEach((error)=>{%>
                <li><%=error.msg%></li>
            <%});%>
        </ul>
    <%}%>
    <form action="/books/<%=book.id%>/edit" method="post">
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%=book.title%>">
        <label for="shortDescription">Short Description:</label>
        <input type="text" name="shortDescription" id="shortDescription" value="<%=book.short_description%>">
        <label for="longDescription">Long Description:</label>
        <textarea name="longDescription" id="longDescription"><%=book.long_description%></textarea>
        <label for="date">Publication Date:</label>
        <input type="date" name="publicationDate" id="date" value="<%=book.publication_date.toISOString().split('T')[0]%>">
        <button type="submit">Update</button>
    </form>
</body>
</html>